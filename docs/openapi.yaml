openapi: 3.1.0
info:
  version: 1.0.0
  title: nodejs-hw-mongodb
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: 'Program for getting contacts from the database'
tags:
  - name: Contacts
    description: 'Contact management operations'
servers:
  - url: https://nodejs-hw-mongodb-s19d.onrender.com
paths:
  # тут будуть посилання на ендпоінти
  /contacts:
    get:
      $ref: ../swagger/paths/contacts/get.yaml
    post:
      $ref: ../swagger/paths/contacts/post.yaml
  /contacts/{id}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
